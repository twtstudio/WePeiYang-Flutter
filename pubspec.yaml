name: we_pei_yang_flutter
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

# 请保证 'package:we_pei_yang_flutter/commons/environment/config.dart'
# 中的 version 和 versionCode 保持一致
version: 4.3.0+113

# 打包代码 flutter build apk --split-per-abi
# flutter build apk --target-platform android-arm,android-arm64 --split-per-abi

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  async: ^2.5.0
  badges: ^2.0.1
  connectivity: ^3.0.6
  dio: ^4.0.0
  cookie_jar: 3.0.1
  dotted_border: ^2.0.0+1
  fluttertoast: ^8.0.8
  flutter_html: ^2.0.0
  flutter_markdown: ^0.6.9+1
  flutter_rating_bar: ^4.0.0
  flutter_svg: ^0.22.0
  flutter_swiper: ^1.1.6
  flutter_screenutil: ^4.0.4
  simple_url_preview: ^3.0.0
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  hive_generator: ^1.1.1
  http_parser: ^4.0.0
  image_cropper: 1.5.0
  image_picker: ^0.8.4+1
  like_button: ^2.0.3
  linkfy_text: ^1.1.5
  intl: ^0.17.0
  location_permissions: ^4.0.1
  package_info: ^2.0.2
  path_provider: ^2.0.4
  photo_view: ^0.13.0
  provider: ^6.0.1
  pull_to_refresh: ^2.0.0
  screenshot: ^1.2.3 # 还没用到
  shared_preferences: ^2.0.7
  simple_html_css: ^3.0.1
  table_calendar: 3.0.2
  transparent_image: ^1.0.0
  url_launcher: ^6.0.10
  webview_flutter: ^2.0.13
  wechat_assets_picker: ^6.2.0
  weathers: ^1.0.2
  extended_tabs: ^2.3.0
  extended_image: ^4.0.0
  flutter_native_image: ^0.0.6+1
  flutter_smart_dialog: ^3.3.5
  # 升级到flutter 2.2.1后，checkbox自带shape属性
  circular_check_box: ^1.0.4
  share_plus: ^4.0.10+1

dependency_overrides:
  meta: ^1.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.2

# The following section is specific to Flutter.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/account/
    - assets/images/account/1.5x/
    - assets/images/account/2.0x/
    - assets/images/account/3.0x/
    - assets/images/account/4.0x/
    - assets/images/schedule/
    - assets/images/lounge_icons/
    - assets/images/lake_butt_icons/
    - assets/svg_pics/
    - assets/svg_pics/lake_butt_icons/
    - privacy/

  fonts:
    - family: Bauhaus
      fonts:
        - asset: assets/fonts/en/Bauhaus-Bold-Italic_6276.ttf
          style: italic

    - family: Swis
      fonts:
        - asset: assets/fonts/en/Swis721-Blk-BT-Black-2.ttf

    - family: ProductSans
      fonts:
        - asset: assets/fonts/en/ProductSans-Bold.ttf
        - asset: assets/fonts/en/ProductSans-Regular.ttf

#    - family: NotoSansSC
#      fonts:
#        - asset: assets/fonts/zh/NotoSansSC-Medium.otf
#
#    - family: PingFangSC
#      fonts:
#        - asset: assets/fonts/zh/PingFangSC-SemiBold.ttf

flutter_intl:
  enabled: true
